package Model;

public enum MeldType {
    PENG, // For 3 same tiles
    GANG, // For 4 same tiles
    EAT, // For 3 in a sequence
    ANGANG, // For 4 same tiles in hand, not shown to other players
    WIN;
}
